import * as types from "../constants/action_types";
const initState = {

};

export default function (state = initState, action = {}) {
  switch (action.type) {
    
    default:
      return state;
  }
}
